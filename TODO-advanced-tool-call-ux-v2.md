# TODO – Advanced Tool Call UX (Inheritance-Based Plan)

- [x] Create `packages/jupyter-ai/src/web-components/tool-call-card/base.tsx` exporting an abstract `ToolCallCardBase` class that encapsulates the current `jai-tool-call` behavior (status management, execution lifecycle, collapsible detail view) and exposes protected hooks (`renderHeaderExtras`, `renderDetails`) for subclasses.
- [x] Implement `packages/jupyter-ai/src/web-components/tool-call-card/registry.ts` with a lightweight registry (`registerToolCallCard`, `getToolCallCard`) that maps `function_name` strings to subclasses of `ToolCallCardBase`, defaulting to the base implementation without adding conditional logic in existing files.
- [x] Refactor `packages/jupyter-ai/src/web-components/jai-tool-call.tsx` so it simply re-exports `DefaultToolCallCard extends ToolCallCardBase` and delegates rendering to the registry-resolved subclass; keep the original logic untouched inside the new base class.
- [x] Add `packages/jupyter-ai/src/web-components/advanced/advanced-plan-summary-card.tsx` and `advanced-plan-worklog-card.tsx`, each extending `ToolCallCardBase` and overriding the protected hooks to render the enhanced UX shown in the screenshots.
- [x] Add `advanced-plan-final-summary-card.tsx` and register a matching tool/card so the final answer summary renders alongside plans.
- [x] Provide `packages/jupyter-ai/src/web-components/advanced/json-utils.ts` (or similar) with a `decodePlanPayload` helper that subclasses share for safe HTML-entity decoding + JSON parsing.
- [x] Expose a `registerAdvancedToolCards()` helper in `packages/jupyter-ai/src/web-components/advanced/index.ts` that registers the advanced subclasses with the registry using their tool function names.
- [x] Update `packages/jupyter-ai/src/web-components/web-components-plugin.ts` to import the registry helper and ensure advanced cards are registered before defining the custom elements, without altering existing sanitizer behavior.
- [ ] Introduce dedicated custom elements (`jai-advanced-plan-summary`, `jai-advanced-plan-worklog`) via `packages/jupyter-ai/src/web-components/tool-call-card/elements.ts`, each wrapping a subclass instance but leaving the original `<jai-tool-call>` tag functional for other tools.
- [ ] Add a thin adapter in `packages/jupyter-ai/src/web-components/index.ts` exporting the new registry + advanced components so downstream code can initialize them explicitly if needed. *(Partially covered by re-export, decide whether a separate adapter module is required.)*
- [ ] Document the initialization sequence in `docs/developers/advanced-tool-calls.md`, clarifying how to register future subclasses without touching core files.
